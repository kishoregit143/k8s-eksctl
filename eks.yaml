apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop-dev
  region: us-east-1

managedNodeGroups:
  - name: roboshop-dev
    instanceTypes: ["m5.large","c5.large","t3.large"]
    desiredCapacity: 3
    minSize: 3
    maxSize: 6
    spot: true